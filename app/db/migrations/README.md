# Database Migrations

Alembic is included in `requirements.txt`, but no migration revisions exist yet.

When Alembic is configured for this project, apply migrations with:

```bash
alembic upgrade head
```
